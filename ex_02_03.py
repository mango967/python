hrs = input('Enter Hours:')
rate = input('Enter rate: ')
pay = round(float(rate) * float(hrs))
print('Pay: ', pay)
#str can't multiply str but float can multiply float
#str can add str if using +
#round function rounds it to integer